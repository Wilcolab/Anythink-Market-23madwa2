/*
Write a robust JavaScript function named `toCamelCase` that converts a string into camelCase.

Requirements:
- Handle strings containing spaces, underscores (_), and hyphens (-)
- Handle uppercase and mixed-case input (e.g., "SCREEN_NAME")
- Trim leading and trailing whitespace
- Validate input and throw a descriptive error if the input is not a string
- Return a properly formatted camelCase string

Examples:
- toCamelCase("first name") should return "firstName"
- toCamelCase("user_id") should return "userId"
- toCamelCase("SCREEN_NAME") should return "screenName"
- toCamelCase("mobile-number") should return "mobileNumber"
*/
